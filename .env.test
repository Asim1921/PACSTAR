# =============================================================================
# PACSTAR TEST Environment Configuration
# =============================================================================
# Copy this file to .env.test and use with docker-compose.test.yml
# Usage: 
#   cp env.test.template .env.test
#   docker-compose -f docker-compose.test.yml --env-file .env.test up -d

# =============================================================================
# MongoDB Configuration (TEST)
# =============================================================================
MONGO_ROOT_USER=pacstar_test_admin
MONGO_ROOT_PASSWORD=pacstar_test_secret_2024
MONGODB_DB=pacstar_test_db

# MongoDB Express (Admin UI)
MONGO_EXPRESS_USER=admin
MONGO_EXPRESS_PASSWORD=admin123

# =============================================================================
# JWT Configuration (TEST)
# =============================================================================
JWT_SECRET_KEY=test-jwt-secret-key-for-testing-only-2024
JWT_REFRESH_SECRET_KEY=test-jwt-refresh-secret-key-for-testing-only-2024
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_MINUTES=10080

# =============================================================================
# Master Admin Bootstrap (TEST)
# =============================================================================
MASTER_ADMIN_USERNAME=master_test
MASTER_ADMIN_EMAIL=master_test@pacstar.local
MASTER_ADMIN_PASSWORD=TestMasterPassword123!

# =============================================================================
# Security Configuration (TEST)
# =============================================================================
SESSION_SECRET_KEY=test-session-secret-key-2024
ALLOWED_HOSTS=["*"]
ALLOW_ORIGINS=["*"]

# =============================================================================
# Environment
# =============================================================================
ENV=dev

# =============================================================================
# Rate Limiting (relaxed for testing)
# =============================================================================
RATE_LIMIT_REQUESTS=200
RATE_LIMIT_WINDOW=60
MAX_FAILED_LOGINS=10
LOCKOUT_TIME_MINUTES=5

# =============================================================================
# Frontend Configuration (TEST)
# =============================================================================
NEXT_PUBLIC_API_URL=http://localhost:8001
BACKEND_URL=http://backend-test:8000

# =============================================================================
# Server Configuration (TEST)
# =============================================================================
SERVER_IP=localhost
SERVER_PORT=8001

# =============================================================================
# OpenStack Configuration (Optional)
# =============================================================================
OPENSTACK_ENABLED=false
OPENSTACK_AUTH_URL=
OPENSTACK_USERNAME=
OPENSTACK_PASSWORD=
OPENSTACK_PROJECT_ID=
OPENSTACK_PROJECT_NAME=
OPENSTACK_USER_DOMAIN_NAME=Default
OPENSTACK_PROJECT_DOMAIN_ID=default
OPENSTACK_REGION_NAME=
OPENSTACK_INTERFACE=public
OPENSTACK_VERIFY_SSL=false

